

const ProfilesPage = () => {
  
  const {user} = JSON.parse(window.localStorage)
  console.log(user)
  return(
    
    <div>
      <h1>Profile page</h1>
      </div>
  )
}

export default ProfilesPage;
